{% extends 'layout.html.twig' %}

{% block body %}
	{{ parent() }}
       
        <section>
            <div id="mensajito">
        {{ include('UnoMainBundle:User:messages/success.html.twig') }}</div>
        <div class="row"><div class="col-md-5 col-lg-offset-2" id="menu"> <h4>USUARIOS</h4> </div><div class=" visible-lg col-md-3"><ul  class="pager" >
     
  </ul></div></div>
	<div class="container " >
            
            <table class="table table-striped table-hover table-condensed"> 
                
                <tr class="info"> 
                    <th><center>#</center></th><th><center> Usuario </center></th><th><center style="color: black"><clas>Nombre</center></th><th><center>ROL</center></th>  <th>  </th></tr> 
{% for producto in productos  %}  
                

<tr> <td><center>{{loop.index}}</center></td><td><center>{{producto.username}}</center></td><td><center>{{producto.nombre}}</center></td> <td><center>{{producto.role }}</center></td> 
			                       
<td>
			                       <a href="{{ path('uno_main_useredit', { id: producto.id }) }}" class="btn btn-sm btn-primary"><li class="glyphicon glyphicon-pencil"></li>
			                            {{ 'Editar'|trans }}
			                        </a>

			                        <a href="{{ path('uno_main_deleteu', { id: producto.id }) }}" id="elimin" class="btn btn-sm btn-danger btn-delete"onclick="return confirm('¿Estás seguro?')"><li class="glyphicon glyphicon-trash "></li>
			                            {{ 'Eliminar'|trans }}
			                        </a>
				               </td>
				                
                </tr> {% endfor %} </table></div>
        </section>
     
       
        
        
        
        
        
        
       
{% endblock %}

